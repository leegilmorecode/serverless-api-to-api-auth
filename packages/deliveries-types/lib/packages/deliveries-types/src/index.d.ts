export declare type CreateDeliveryInput = {
  correlationId: string;
  orderId: string;
};
export declare type CreateDeliveryOutut = {
  correlationId: string;
  orderId: string;
  deliveryId: string;
};
